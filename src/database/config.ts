/**
 * Configuration settings for the SQLite database.
 */
const databaseConfig = {
  name: 'hoursWorked.db',
  version: '1.0',
  displayName: 'SQLite Hours Worked Database',
  size: 200000,
};

export default databaseConfig;
